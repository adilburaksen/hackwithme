# Every route is prerendered to its own static document at build time
# (scripts/prerender.mjs): /about/index.html, /writing/index.html, etc.
# Netlify serves those for /about and /about/ automatically, so no rewrites are
# needed. Any unmatched path falls through to a real 404 with the prerendered
# 404 page (static files are matched before this rule).
/*    /404.html    404
